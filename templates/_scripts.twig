<script src="js/jquery-3.1.1.js"></script>
<script src="js/jquery.sticky.js"></script>
<script>
	// $('i').mouseover(e => {
	// 	$(e.currentTarget).css({
	// 		'color': 'yellow'
	// 	});
	// });
	// $('i').mouseleave(e => {
	// 	$(e.currentTarget).css({
	// 		'color': '#FFF'
	// 	});
	// })
	$(document).ready(function(){
    $(".title").sticky({topSpacing:-2, 'zIndex': 100, responsiveWidth: true});
  });

  $(document).ready(function(){       
   var scroll_start = 0;
   var startchange = $('.title');
   var offset = startchange.offset();
    if (startchange.length){
   $(document).scroll(function() { 
      scroll_start = $(this).scrollTop();
      if(scroll_start > offset.top) {
          $(".my-nav").css('background-color', '#0F1626');
       } else {
          $('.my-nav').css('background-color', 'rgba(0, 0, 0, .25)');
       }
   });
    }
});
</script>